# ext/

Weak-dependency extensions for CTBase.jl. Each extension lives in its own
subdirectory with a manifest file and is loaded automatically by Julia when its
trigger package is available — none are loaded at package import time.

Conventions: [control-toolbox Handbook](https://github.com/control-toolbox/Handbook).
