# CompatHelper v3.5.0
name: CompatHelper
on:
  schedule:
    - cron: 0 0 * * *
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  CompatHelper:
    uses: control-toolbox/CTActions/.github/workflows/compat-helper.yml@main
