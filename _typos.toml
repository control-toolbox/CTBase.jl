[default]
locale = "en"
extend-ignore-re = [
    "adnlp_backen",
    "ipopt_backen",
    # Common variable names in our codebase
    "strat",
    "Strat",
    "mis-",
]

[files]
extend-exclude = [
    "*.json",
    "*.toml",
    "*.svg",
]
